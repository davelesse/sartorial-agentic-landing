[pytest]
# `strict` : chaque test asynchrone doit porter @pytest.mark.asyncio. En mode
# `auto`, un test async oublié passerait sans jamais être attendu — donc vert
# sans avoir rien vérifié. Sur des tests de concurrence, c'est inacceptable.
asyncio_mode = strict
testpaths = .
